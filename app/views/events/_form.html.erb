<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :start_date %><br>
    <%= f.date_select :start_date %>
  </div>
  <div class="field">
    <%= f.label :end_date %><br>
    <%= f.date_select :end_date %>
  </div>
  <div class="field">
    <%= f.label :capacity %><br>
    <%= f.number_field :capacity %>
  </div>

<%= f.fields_for :windows do |windows_form| %>
  <li>
    <%= windows_form.label :start_time %>
    <%= windows_form.datetime_select :start_time %>

    <%= windows_form.label :capacity %>
    <%= windows_form.text_field :capacity %>
  </li>
<% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
